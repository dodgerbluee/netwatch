services:
  netwatch:
    image: ghcr.io/dodgerbluee/netwatch:latest
    container_name: netwatch
    restart: unless-stopped
    environment:
      - TZ=America/Chicago
    ports:
      - "8099:8099"
    volumes:
      - netwatch-data:/data

volumes:
  netwatch-data:
